import { FunctionComponent } from "react";
import { ListCoinsSection } from "./listCoins.styles";

export interface ListCoinsProps {}

const ListCoins: FunctionComponent<ListCoinsProps> = ({}) => {
  return <ListCoinsSection></ListCoinsSection>;
};

export default ListCoins;
