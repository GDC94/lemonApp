import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerCoins: {
      backgroundColor: "#000522",
      alignItems: "center",
      flex:1,
      margin: 50,
      padding: 20,
      borderRadius: 20,
   
  },
  Loader: {
    marginVertical: 20,
    alignItems: "center"
  }
});
